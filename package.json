{
  "name": "fatburn-alert",
  "version": "1.0.0",
  "description": "Strava workout fat burn notification service",
  "main": "src/index.ts",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Skipping build - Vercel handles API routes'",
    "test": "npx tsx src/test-local.ts",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
